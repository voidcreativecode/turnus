# Tailwind Lint Warnings - Explicação

## ⚠️ Warnings Conhecidos e Intencionais

### 1. `rounded-[2rem]` vs `rounded-4xl`

**Warning**: "The class `rounded-[2rem]` can be written as `rounded-4xl`"

**Motivo para manter**:

- O valor `2rem` (32px) é específico do design system do Turnus
- Mantém consistência visual exata com o design original
- Evita confusão com outros valores de border-radius

**Arquivos afetados**:

- `components/Features.tsx` (linhas 20, 59, 69, 81, 91, 117)

### 2. `rounded-[1.5rem]` vs `rounded-3xl`

**Warning**: "The class `rounded-[1.5rem]` can be written as `rounded-3xl`"

**Motivo para manter**:

- Valor específico para o mockup do telefone
- Mantém proporções exatas do design

**Arquivos afetados**:

- `components/Features.tsx` (linha 92)

### 3. `bg-gradient-to-b` vs `bg-linear-to-b`

**Warning**: "The class `bg-gradient-to-b` can be written as `bg-linear-to-b`"

**Motivo para manter**:

- `bg-gradient-to-b` é a sintaxe padrão do Tailwind CSS
- `bg-linear-to-b` não é uma classe válida no Tailwind padrão
- Este warning pode ser um falso positivo do linter

**Arquivos afetados**:

- `components/Features.tsx` (linha 82)

### 4. Radial Gradient Spacing

**Warning**: "The class `bg-[radial-gradient(circle_at_center,_var(--tw-gradient-stops))]` can be written as `bg-[radial-gradient(circle_at_center,var(--tw-gradient-stops))]`"

**Motivo para manter**:

- Ambas as sintaxes são válidas
- O espaço após a vírgula melhora a legibilidade
- Não afeta o resultado final

**Arquivos afetados**:

- `components/Pricing.tsx` (linha 106)

## 🎯 Decisão de Design

Optamos por manter estes valores customizados porque:

1. **Fidelidade ao Design**: Mantém exatamente os valores especificados no design original
2. **Consistência**: Evita confusão entre valores similares mas não idênticos
3. **Manutenibilidade**: Valores explícitos são mais fáceis de entender e modificar
4. **Sem Impacto**: Não há diferença de performance ou funcionalidade

## 🔧 Como Suprimir (se necessário)

Se você quiser suprimir estes warnings, adicione ao `.eslintrc.json`:

```json
{
  "rules": {
    "tailwindcss/no-custom-classname": "off"
  }
}
```

Ou crie um arquivo `tailwind.config.js` com valores customizados:

```js
module.exports = {
  theme: {
    extend: {
      borderRadius: {
        "turnus-card": "2rem",
        "turnus-phone": "1.5rem",
      },
    },
  },
};
```

## ✅ Conclusão

Estes warnings são **informativos** e não indicam erros. O código está correto e funcional. Mantemos os valores customizados por motivos de design e clareza.

---

_Documentado para referência futura da equipe_
